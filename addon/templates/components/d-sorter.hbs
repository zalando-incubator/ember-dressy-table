{{yield (hash
                table=(hash
                        settings=table.settings
                        columns=table.columns
                        state = table.state
                        sorting = table.state.sorting
                        data = sortedData
                )
        )}}